# Detector parameters frozen for the M2 evaluation run.
# Selected on the dev split only; see docs/validation/2026-09-13-pocket-detector-m2.md.
# NOT every field is written out. `deck_evidence_tol_m` and
# `upper_band_points` are absent and fall back to the code's defaults
# (0.006 m and 0, meaning "follow min_band_points"), because the loader
# rejects unknown keys but does not check for missing ones. Adding them
# here would break the replay test, which asserts this file still matches
# the params recorded in the two archived runs. Changing a default in the
# code therefore changes this "frozen" set silently -- see Task 7 of
# docs/plans/2026-09-13-pocket-evidence-restructure.md.
band_margin_m: 0.01
cell_m: 0.01
floor_z_m: 0.02
front_margin_m: 0.05
max_plane_candidates: 3
max_plane_residual_m: 0.015
min_band_points: 100
min_plane_points: 300
occluded_front_frac: 0.5
open_behind_frac: 0.3
plane_inlier_m: 0.02
range_max_m: 5.0
range_min_m: 0.8
ransac_iterations: 200
seed: 20260913
width_mismatch_frac: 0.2
